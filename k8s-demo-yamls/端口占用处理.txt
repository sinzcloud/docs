# 查看某端口（例如 8080）
netstat -ano | findstr :8080

# 强制结束掉该进程
taskkill /PID 12345 /F




占用 80 的就是 IIS 相关服务：

WAS = Windows Process Activation Service
W3SVC = World Wide Web Publishing Service（IIS Web 服务）

所以 PID=4 是 HTTP.sys，被 IIS 通过 svchost 注册了 80。


立即释放 80
net stop w3svc
net stop was